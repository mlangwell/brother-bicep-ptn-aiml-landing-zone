{
  "name": "bicep-ptn-aiml-landing-zone",
  "version": "1.0.0",
  "private": true,
  "description": "Local validation entry points for the landing zone: npm test runs the local gate, npm run lint runs the pinned PowerShell style checks.",
  "type": "module",
  "scripts": {
    "test": "pwsh -NoProfile -NonInteractive -File ./scripts/github/Test-Local.ps1",
    "lint": "pwsh -NoProfile -NonInteractive -File ./scripts/github/Test-CodeStyle.ps1"
  }
}